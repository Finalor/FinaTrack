@inherits LayoutComponentBase

<MudThemeProvider IsDarkMode="true"/> <!-- Fournit la configuration de thème pour les composants MudBlazor -->
<MudPopoverProvider/> <!-- Gère les composants popover, permettant d'afficher des info-bulles, menus déroulants, etc. -->
<MudDialogProvider/> <!-- Facilite l'utilisation des boîtes de dialogue modales -->
<MudSnackbarProvider/> <!-- Fournit des notifications snackbar pour des messages brefs ou des alertes -->

<MudLayout>
    <MudAppBar>
        <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" Edge="Edge.Start"
                       OnClick="@(_ => DrawerToggle())"/>
        FinaTrack
    </MudAppBar>
    <MudDrawer @bind-Open="@_drawerOpen">
        <NavMenu/>
    </MudDrawer>
    <MudMainContent>
        @Body
    </MudMainContent>
</MudLayout>

@code {

    /// <summary>
    /// État du menu (ouvert ou fermé).
    /// </summary>
    bool _drawerOpen = true;

    /// <summary>
    /// Basculer l'état du menu entre ouvert et fermé.
    /// </summary>
    void DrawerToggle()
    {
        _drawerOpen = !_drawerOpen;
    }

}